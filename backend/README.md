# Backend

NestJS API for the Pomodoro take-home. See the root [README](../README.md) and [design.md](../design.md).

```bash
# Postgres on localhost:5433 (docker compose)
npm install
npm run start:dev
npm test
npm run test:e2e
```
